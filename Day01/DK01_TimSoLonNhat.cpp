#include <bits/stdc++.h>

using namespace std;

int main()
{
    // Khai báo 2 biến a, b
    int a, b;

    // Nhập 2 số a, b;
    cin >> a >> b;

    // Kiểm tra số lớn hơn
    if (a >= b) cout << a;
    else cout << b;
    return 0;
}
